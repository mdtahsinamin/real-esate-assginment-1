/* eslint-disable react/prop-types */
const PolygonCTA = ({ d, stroke, strokeOpacity, strokeWidth }) => {
  return (
    <path
      d={d}
      stroke={stroke}
      strokeOpacity={strokeOpacity}
      strokeWidth={strokeWidth}
    />
  );
};

export default PolygonCTA;
